# ROE import regression corpus

This synthetic corpus prevents known English and Turkish scope patterns from regressing. It is not a claim about accuracy on real customer contracts.

Run it from the repository root:

```bash
python benchmarks/run.py
```

Every case uses reserved example domains or private address space. Contributions should add wording variants seen in sanitized documents without including customer names, live targets, credentials, or proprietary text.
